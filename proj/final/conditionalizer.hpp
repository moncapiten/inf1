#ifndef CONDITIONALIZER_HPP
#define CONDITIONALIZER_HPP

#include <iostream>
#include <unordered_map>
#include <vector>
#include <string>
#include <cmath>
#include <algorithm>
#include "nodes.hpp"

using namespace std;

// Forward declarations
//struct BayesianNode;
//class BayesianNetwork;
/*
typedef unordered_map<int, int> Assignment; // node ID to state index

// Helper to compute the joint probability of a full assignment
double computeJointProbability(const BayesianNetwork& net, const Assignment& assignment);

// Recursive function to enumerate all full assignments
void enumerateAssignments(const BayesianNetwork& net, int index, Assignment& current,
                          vector<Assignment>& results);

// Conditional probability P(target = value | given = value)
double computeConditionalProbability(const BayesianNetwork& net,
                                     int targetID, int targetStateIdx,
                                     int givenID, int givenStateIdx);


double computeConditionalProbability(const BayesianNetwork& net,
                                     const string& A, const string& a,
                                     const string& B, const string& b);
*/

#endif // CONDITIONALIZER_HPP